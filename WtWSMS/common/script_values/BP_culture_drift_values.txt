drift_start_year_0_period = {
	value = scope:drift_start_year
}
drift_start_year_1_period = {
	value = scope:drift_period_years
	add = scope:drift_start_year
}
drift_start_year_2_period = {
	value = scope:drift_period_years
	multiply = 2
	add = scope:drift_start_year
}
drift_start_year_3_period = {
	value = scope:drift_period_years
	multiply = 3
	add = scope:drift_start_year
}
drift_start_year_4_period = {
	value = scope:drift_period_years
	multiply = 4
	add = scope:drift_start_year
}
drift_start_year_5_period = {
	value = scope:drift_period_years
	multiply = 5
	add = scope:drift_start_year
}
drift_start_year_6_period = {
	value = scope:drift_period_years
	multiply = 6
	add = scope:drift_start_year
}
drift_start_year_7_period = {
	value = scope:drift_period_years
	multiply = 7
	add = scope:drift_start_year
}
number_of_counties_with_minority = {
  value = 0
  every_culture_county = {
    if = {
      limit = { county_has_specific_minority_culture_trigger = { CULTURE = scope:check_minority_culture } }
      add = 1
    }
  }
}
half_number_of_counties_with_minority = {
  value = number_of_counties_with_minority
  divide = 2
  floor = yes
}
number_of_drift_culture_counties_with_minority = {
  value = 0
  scope:drift_culture = {
	every_culture_county = {
		if = {
		limit = { county_has_specific_minority_culture_trigger = { CULTURE = scope:check_minority_culture } }
		add = 1
		}
    }
  }
}