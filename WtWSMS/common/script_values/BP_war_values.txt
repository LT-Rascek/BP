#Cultural War Costs
cultural_unification_cb_prestige_cost = {
	value = 0
	if = {
		limit = {
			NAND = {
				scope:attacker.faith.religious_head = scope:attacker
				scope:defender.faith = scope:attacker.faith
			}
		}
		add = {
			value = 200
			desc = CB_BASE_COST
		}
	}
	if = {
		limit = {
			exists = scope:defender
			scope:attacker.primary_title.tier < scope:defender.primary_title.tier
		}
		multiply = {
			value = 1.5
			desc = CB_COST_ATTACKING_SUPERIOR
		}
	}
	if = {
		limit = {
			exists = scope:defender
			scope:defender ?= scope:attacker.culture.culture_head
		}
		multiply = {
			value = 1.5
			desc = CB_COST_ATTACKING_CULTURE_HEAD
		}
	}
	multiply = common_cb_prestige_cost_multiplier
}

heritage_unification_cb_prestige_cost = {
	value = 0
	if = {
		limit = {
			NAND = {
				scope:attacker.faith.religious_head = scope:attacker
				scope:defender.faith = scope:attacker.faith
			}
		}
		add = {
			value = 500
			desc = CB_BASE_COST
		}
	}
	if = {
		limit = {
			exists = scope:defender
			scope:attacker.primary_title.tier < scope:defender.primary_title.tier
		}
		multiply = {
			value = 1.5
			desc = CB_COST_ATTACKING_SUPERIOR
		}
	}
	multiply = common_cb_prestige_cost_multiplier
}

#### Unification War Stuff
unification_war_fame_base = major_prestige_value

#Counts up number of available subjugations the player gets
total_available_subjugations = {
	value = 1
	if = { #One more if your religion is the right type
		limit = { this.faith = { has_doctrine_parameter = increased_subjugation_limit } }
		add = 1
	}
	if = { #One more if you are in a migration
		limit = {
			general_situation_regional_parameter_trigger = {
				CHARACTER = this
				PARAMETER = increased_subjugation_limit
			}
		}
		add = 1
	}
	if = { #One less if your religion is the wrong type
		limit = { this.faith = { has_doctrine_parameter = decreased_subjugation_limit } }
		subtract = 1
	}
	if = { #One more if your culture is the right type
		limit = { this.culture = { has_cultural_parameter = increased_subjugation_limit } }
		add = 1
	}
	if = { #One less if your culture is the wrong type
		limit = { this.culture = { has_cultural_parameter = decreased_subjugation_limit } }
		subtract = 1
	}
	if = { #One for Legendary Characters, given the right government
		limit = {
			OR = {
				this = { government_has_flag = government_is_tribal }
				this = { government_has_flag = government_is_clan }
			}
			
			any_character_trait = { has_trait_flag = increased_subjugation_limit }
		}
		add = 1
	}
	min = 1
}

#### Generic War Values
bp_minor_war_cb_prestige_experience_gain = 10
bp_minor_war_prestige_white_peace = -100
bp_minor_war_prestige_allies = 100
bp_minor_war_prestige_main = 250
